//! Setup panel — REMOVE: interactive setup wizard has been moved to CLI (`obenmatrix setup`).
//!
//! The TUI no longer contains setup logic. When config is missing, the splash
//! panel displays a message prompting the user to run `obenmatrix setup`.
