# Domain Docs

This repo uses a single-context layout with `CONTEXT.md` at the repo root.

## Rules for Skills

Skills reading `CONTEXT.md` should load it from the repo root.
Skills reading ADRs should look in `docs/adr/`.
